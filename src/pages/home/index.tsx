/** @jsxRuntime classic */
/** @jsx jsx */
import { css, jsx } from '@emotion/react';
import React from 'react';

const HomePage: React.FC = () => {
  const styles = {
    container: css``,
  };
  return <div css={styles.container}>home works</div>;
};

export default HomePage;
